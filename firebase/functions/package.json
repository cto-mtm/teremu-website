{
  "name": "teremu-functions",
  "description": "Cloud Functions for the Teremu website forms",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "preview:emails": "npm run build && node scripts/preview-emails.mjs"
  },
  "dependencies": {
    "@google-cloud/recaptcha-enterprise": "^6.4.2",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0",
    "nodemailer": "^6.10.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.17",
    "typescript": "^5.7.0"
  }
}
