# Legacy URL Redirects

Track every 301 redirect added to `www/nuxt.config.ts` `routeRules` here so the
two stay in sync. Add a row whenever an old URL from a prior site or domain
restructure needs to point at a new path.

| Old URL | New URL | Notes |
|---|---|---|
| `/example-old` | `/` | Example row — delete once you add a real redirect. |
